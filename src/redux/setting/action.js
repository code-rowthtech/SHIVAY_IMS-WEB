import { changeProfilePasswordConstants } from './constants';

export const ChangeProfilePasswordAction = (data) => ({
    // return {
    type: changeProfilePasswordConstants.CHANGE_PROFILE_PASSWORD,
    data,
    // };
});
